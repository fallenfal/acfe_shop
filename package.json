{
  "name": "acfe-shop",
  "private": true,
  "description": "Optional local build helper (production uses Dockerfile on Railway)",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend"
  }
}
